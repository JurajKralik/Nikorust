use crate::Nikolaj;
use crate::helpers::construction::*;


pub fn construct_barracks(bot: &mut Nikolaj) {
    let position = get_placement_on_grid(bot);
    println!("Position: {:?}", position);
    return;
}